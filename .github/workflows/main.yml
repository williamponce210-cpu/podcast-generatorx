name: Generate Podcast Feeds

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Generate Podcast Feed
        uses: williamponce210-cpu/podcast-generator@v1
